{
  "name": "hanasaki", // 自定义 Worker 名称（唯一）
  "compatibility_date": "2025-12-23", // 兼容日期（保持当前日期）
  "main": "src/index.ts", // Worker 脚本入口（必填）
  // 静态资产配置：Wrangler 4.x 要求为单层对象，仅保留必填的 directory
  "assets": {
    "directory": "./", // 静态资源根目录（你的项目根目录，必填）
    "include": ["*.html", "pictures/**"], // 要部署的文件/文件夹
    "exclude": ["src/**", "node_modules/**"] // 排除不需要部署的目录
  }
}